package test;

public class Program {
    static Double distance(Point_2D a, Point_2D b) {
        return Math.sqrt(Math.pow(a.getX() - b.getX(),2) + Math.pow(a.getY() - b.getY(), 2));
    }
    public static void main(String[] args) {    
        
    }    
}
